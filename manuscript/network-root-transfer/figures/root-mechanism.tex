\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/root-mechanism.pdf}
\caption{\textbf{Blind probes, nonlinear readout, and compressed-return
reconstruction.}
\textup{(a)} The gray parabola is the exact singular curve in
\((\widehat X,\widehat Y)=(\alpha X,\alpha Y)\); the colored one-sided RFDE
traces and dotted preparation tails are schematic.  The marked point is the
selected section match on \(X=0\).  Equality of the complete histories follows
from \cref{lem:shared-graph-trace}, not from the drawing, and the exact
finite-section root remains completion dependent.
\textup{(b)} The solid chain records proved identities and the uniform
finite-scale response estimate; only its spatial layout is schematic.
Atomwise stationary-row neutrality gives exact projection blindness at every
unreduced history, while \(\sum_kR_k=0\) makes the perturbation a pure
redistribution.  Two distinct merged delay locations make
\(\mathsf S_N\) onto \(E_N\) with the displayed sharp right inverse, whereas
one merged delay location gives the gray no-go branch.  Limiting normalized
root responses on a spanning probe family determine only the compressed
transverse return covector; finite-scale responses approximate its
evaluations.  The reconstruction is
\(\mathfrak r_N(z)=\Lambda_N(\mathsf Q_NA_Nz)\), not the full network or
return operator.  Curvature and recovery sensing (blue solid and orange
dashed cues) are two separate realization profiles of this abstract chain.}
\label{fig:root-mechanism}
\end{figure}
