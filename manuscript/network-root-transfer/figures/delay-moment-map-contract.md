# Figure plan: delay moment and connection sensitivity

- Reader question and result served: How can a perturbation of delayed coupling whose stationary projection is identically zero contribute to the derivative of a scalar connection parameter?
- Status: `MIXED`. The linear maps and equalities are exact; the box placement and arrows are schematic; the final connection-parameter derivative formula is conditional and asymptotic.
- System, parameters, coordinates, and time convention: structural direction `E=(E_k)` in the stated perturbation space, fold-time first moment, transverse generator `A_N`, and the scalar Fredholm functional.
- Objects, directions, and representative choices: the top branch records the exact projected derivative `D_E(Pi_N F)=0`; the lower branch records `E -> S_N E -> A_N^{-1}S_N E -> Lambda_N(E) -> D_E mu_c^g`. A dashed branch records the merged-one-delay cancellation.
- Panels and reading order: one left-to-right panel.
- Line, marker, fill, color, and arrow meanings: solid arrows are proved maps; a dashed arrow marks the one-delay alternative; blue boxes are transverse quantities; the final orange box is the scalar connection derivative. Color is redundant with labels and border styles.
- What the figure must not imply: the projection is not a dynamically closed quotient; `Lambda_N` need not be nonzero for every allowed direction; the final connection derivative requires the invariant-manifold and comparison hypotheses; the diagram is not a reconstruction of the full network.
- Caption draft: Perturbations of delayed coupling satisfy an exact stationary-projection identity, but their first delay moment can force the transverse equation. The uniformly bounded transverse inverse and the collective Fredholm functional give the covector `Lambda_N`. With one merged delay, the first-moment map vanishes on the stated perturbation space. The linear maps are exact; the connection statement is conditional and asymptotic; placement is schematic.
- Editable source: Matplotlib vector PDF generated by `delay_moment_map.py`.
- Final placement and size: full text width near the first statement of the delay-moment map.
- Checks needed before delivery: signs and factors in `S_N`; arrow direction; one-delay qualifier; final-size label legibility; grayscale distinction; embedded fonts; no implication that the projection is a quotient.
