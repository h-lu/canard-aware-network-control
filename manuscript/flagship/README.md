# Flagship successor manuscript

This directory is the theorem-facing LaTeX draft for the finite-network and
physical-pulse successor paper.  It is deliberately separate from
`manuscript/jns`, which is the completed two-module complete-history paper.

Current status: **active research draft, not submission ready.**  The finite-
network transverse inverse, structural one-gap theorem, shared-resource
Dobrushin instance, scalar periodic objects, center-inner Floquet index, and
two-output response are theorem-level inputs.  The Fourier-cokernel to
continuous Route-C adjoint bridge and the fixed-time wide pulse-parameter
Taylor--Bernstein model are now also rigorous inputs.  A separately replayed
certificate proves a unique Route-C event, its fourth-order event-time graph,
and a continuous common-event history tube on the full pulse interval.  A
second replayed certificate directly encloses the first variational equation,
event-time derivative, and translated complete-history derivative on that
same interval.  A third 192-bit certificate fixes the physical real right
gauge and proves that the normalized Route-C functional action on this
derivative lies in
\([-258.746521015805,-245.253478984195]\) throughout the interval.  This is
the action of one fixed center functional, not by itself the stable-sheet-gap
derivative.  The source-bound Stage-5F parent proved the earlier coarse
conditional bound \(\|P_sD_J\kappa\|_Y\le14.727579\) and
\(H'\subset[-494.387771,-9.612229]\); those frozen values remain part of the
audit history but have now been sharpened by Stages 5G-a and 5G-b.  Stage 5G-a
proves strict opposite signs of the endpoint functional coordinates in the
identical centered chart and bounds both endpoint stable coordinates.  Using
the direct physical-column norm, Stage 5G-b proves
\(\|P_sD_J\kappa\|_Y\le5.979324\) and the full-interval cone
\(P_s\kappa(I_J)\subset\overline B_Y(0,47/5000)\cap\ker f_{\rm phys}\).
Consequently, a future graph defined on that certified ball with
\(\sup\|D\psi\|\le16\) would satisfy
\(H'\subset[-354.415700,-149.584300]\).  The affine section splitting is
global, so no separate ambient-chart containment estimate is required.  The
physical-time correlated base-orbit \(uu\) stable-output block is also proved
strictly below its design target.  A separate direct common-row certificate
now proves, for the selected near-one-period phase-fixed inner linear map,
\(\|AP_s\|\le0.009896427481610001<0.1\), and hence
\(\|A_s^n\|\le0.1^n\) with \(K_s=1\).  Stage 4L is frozen at result SHA
`672f92c7c456a54f39afab7d2a5f92b783311cc0ee5341a4d2e72a588039017e`
and canonical SHA
`b27631b62d437bf12431d751d4dad79570a03fa9a66fc0115d90f49768e058fc`;
all \(31\) tests pass, including a genuine fresh replay.

This discrete linear result does not close the stable graph.  The exact
Stage-4O moving-event Hessian now fixes both event-time derivatives,
complete-history translation, and the order of the event and spectral
projections.  It also exposes a genuine regularity gate: the present
one-period argument does not establish a \(C^2\) moving return on a ball of
arbitrary continuous histories; this is not a non-\(C^2\) theorem for the
particular map.  The general Stage-4R theorem proves the sufficient threshold
\(T_->k\tau_{\max}\) for a \(C^k\) selected complete-history return in any
finite-dimensional fixed-delay RFDE under its stated initial-chart
regularity, common solution/event tube, endpoint-sign, positive-speed, and
terminal-chart containment hypotheses; no no-earlier-hit premise is needed
for selected-branch smoothness.  A direct selected return identifies the
intrinsic periodic-orbit stable-set germ only with the stated invariant
phase-isolating section, common intervening tube, retained-domain, and
recurrent-hit hypotheses, without first proving \(Q=P^m\).

The selected near-two-period center clears the \(C^2\) smoothing threshold
by more than \(14.010740\).  Stage 4U proves a common \(C^2\) selected
complete-history hit on the explicit arbitrary-\(C^0\) scale
\(\lambda_0=9\times10^{-31}\).  Stage 4V's orbit-aware weighted-energy
estimate enlarges the proved closed scale to \(1.2\times10^{-8}\), with a
strictly larger open scale \(1.25\times10^{-8}\); \(\lambda=1\) remains
unproved.  Stage 4T proves directly that
\(DQ_Y(Y_*)=A^2\); the fixed two-step splitting and rates are proved as well.
Stage 4W supplies the exact split quotient and signed-bimeasure residual
theorem and proves a qualitative local \(C^2\) selected-map stable graph, but
no effective radius or any of the six numerical Hessian caps.
Stage 4P proves that a future simultaneous
six-block box \((1,10,1000,5,10,1000)\) closes the split graph arithmetic
with Perron bound below \(0.193086\).  After the proved physical-coordinate
adapter, it would give graph height below \(0.001623187\), strict endpoint
margins above \(0.0180540\) and \(0.0131604\), and
\(H'\subset[-260.794147,-243.205853]\).  The signed Stage-4Q
\(n_{\rm step}=120,180,240\) finite-section pilot lies comfortably inside all
six caps, but is explicitly nonrigorous.  The active inner proof chain is
enlargement to the preferred graph ball, graph-transform return-domain
containment, all six directed continuous-history Hessian blocks, and either the direct-return
stable-set hypotheses or \(Q=P^2\) on nested domains.  No-earlier-hit control
is needed only for a first-return or ordinal label.  Once that source-bound
chain closes, the quantitative graph and unique selected crossing follow
from already closed arithmetic; outer-side attachment, routing, biological
onset, and safety remain open.

The source-bound Stage-4N feasibility pilot is strictly diagnostic.  On the
preferred anisotropic ball its two cancellation-blind scalar Gronwall routes
fail by directed lower values \(11.971029\ldots\) and
\(32.257662\ldots\) decimal orders (reported as \(11.9710\) and
\(32.2577\)).  It records the sufficient future signed event-aligned target
\(K_{\rm ret}<188.9122238810816\), numerically \(188.9122238811\), but no
kernel bound.  Its result SHA is
`5e7214a2f5ba8ca22649c677a1d054b32342b5cc25966bd8e1da7600c605f1de`.
The later split analysis proves that this scalar target is only a sufficient
return-domain route and is not necessary for graph closure.  Every nonlinear
full-ball return, event-window, directed Hessian, graph, crossing, onset,
routing, capture, and safety flag remains false.

For the outer return, the exact 21-word/Duffy reduction and the global
exact-orbit relative-residual bounds for \(F,G\) are proved.  Separate
triangular propagation through \(H,L\) is rigorously too coarse; the
combined advanced-row identity, its strict instantaneous Green/boundary
bounds, and exact coefficient/phase-ratio budgets are also proved.  The
first global delayed-tensor and output-row runs were audit-retracted after
an endpoint check found that their \(12{,}000\)-patch geometry omitted a
second nonempty terminal-clipped layer.  The corrected Stage-3G-v2 theorem
now covers all \(730+40\) rectangles and \(12{,}320\) patches and closes the
directed residual and Green/boundary bootstrap.  Stage 3H v2 then proves
strict center-guide phase-combined output-row sizes.  The old v1 values
remain inadmissible.  Stage 3I now covers all \(25{,}600\) continuous
signed-density rectangles and proves exact phase-fixed reduced-history
return-row bounds \(0.550516\) and \(0.028281\), hence arbitrary reduced-
\(C^0\) linear contraction.  Its auxiliary \(0.01\) closeness target fails,
but is not the contraction gate.  Stage 6A upgrades this linear parent to a
source-bound, nonzero nonlinear outer local return and attracting tube.  On
the exact phase-zero section its radius is \(r_6=10^{-335}\), the compatible
full complete-history tube has radius \(R_6=10^{-3}\), and the nonlinear
return constant satisfies \(\Lambda_6\le0.561839\).  The same certificate proves a
\(C^2\) local event map and no earlier hit of that local section.  This is a
microscopic local attraction theorem, not biological capture: the
\(J=0.32\) history fails ambient-to-section domain containment, and reaching
its \(2.637079\times10^{-5}\) ambient-proximity scale requires
\(C_P<17044.784546<17044.785\).  Entry, outer-side attachment, capture,
routing, and onset remain open.

The current build contains the collective--transverse proof
mechanism, the proved Route-C event theorem, the conditional stable-sheet
completion theorem, the proved event-aligned first-derivative theorem, the
phase-invariant oriented-quotient lemma, the proved fixed-center
physical-phase action, the proved selected discrete inner stable-power
proposition, the general eventually-smooth selected-return theorem, the exact
moving-event Hessian, and the conditional two-return graph/crossing arithmetic.
It also
contains the microscopic nonlinear outer local-return theorem, the quantitative
frequency--amplitude--threshold-offset inverse, and local network root-erosion
formulas.  Those formulas acquire a
biological safety interpretation only after the still-open uniform routing
hypotheses are proved.  An explicit evidence ledger records this boundary.

Build from this directory with `make`.  The authoritative claim boundary and
dependency ledger remain in `../flagship-successor-outline.md` until every
release gate is closed.
