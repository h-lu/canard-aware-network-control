# Stage 5F: physical action to a centered stable-gap slope

Status: **PROVED conditional implication; all quantitative graph antecedents
in the registered chart remain OPEN.**  This stage closes the rank-one
linear-algebra bridge between the released Stage-5E physical functional action
and a future nonlinear stable-sheet certificate.  It neither constructs that
sheet nor proves pulse onset.

## 1. One section, one norm, one Grushin pair

The ambient history space and Route-C section tangent space are

\[
 Y=C([ -\tau_{\max},0],\mathbb R)\times\mathbb R,
 \qquad
 \|(\phi,w)\|_Y=\max\{\|\phi\|_\infty,|w|\},
\]

\[
 \Sigma=\{y\in Y:y_v(0)=0\},
\]

with the inherited $Y$ max norm.  The Stage-3/4D Grushin eigencolumn and
functional, after the Stage-5E physical phase rotation, are denoted by
$q=q_{\rm phys}\in\Sigma$ and $f=f_{\rm phys}$, with $f(q)=1$.  Set

\[
 P_s=I-qf\colon\Sigma\longrightarrow\Sigma.
\]

The numerical gate $\|D\psi\|\le16$ below has meaning only for the operator
norm from $(P_s\Sigma,\|\cdot\|_Y)$ to the scalar coordinate defined by this
same $f$.  It cannot be imported from a finite left row, a differently
scaled eigencolumn, or another history norm.  A future Stage-4 graph
certificate must byte-bind this normalization, section and norm.

## 2. Centered chart and selected event

Stage 5C validates a unique Route-C crossing in a **selected late time
window**.  Let $K(J)$ be its actual event-aligned history and let $X_*$ be
the inner periodic-orbit reference history at the same phase-zero section.
The pinned Stage-5B through Stage-5E chain fixes

\[
 J_0=\frac{2409}{8000},\qquad h=\frac{3}{40000},\qquad
 I_J=\left[\frac{6021}{20000},\frac{753}{2500}\right],
 \qquad J=J_0+h\xi,\quad \xi\in[-1,1].
\]

The local graph coordinate is the centered history

\[
 \kappa(J)=K(J)-X_*\in\Sigma,
 \qquad D_J\kappa(J)=D_JK(J).
\]

Define the centered stable-sheet gap by

\[
 H(J):=f(\kappa(J))-\psi(P_s\kappa(J)).        \tag{2.1}
\]

Then $\kappa(J)$ lies on the local stable sheet if and only if $H(J)=0$; this
does not assert that the pulse curve is already on the sheet.  The gap is not
an uncentered graph in $K$.  Applying this chart also requires a new
proof that the ambient chart contains $\kappa(I_J)$ and that the domain of
$\psi$ contains $P_s\kappa(I_J)$.  This is domain containment, not the claim
that the pulse curve itself lies on the stable sheet.

The selected late-window event has not been proved to be the ordinal third
crossing.  The binary64 180-step third-return pilot uses an unvalidated finite
left row, so its derivative near $-14$ neither calibrates nor contradicts the
Stage-5E complete-history action near $-252$.  The local Poincare first return
used in the inner graph construction and the physical event ordinal are
distinct statements.

## 3. Exact projection identity

Stage 5E formed the correlated residual

\[
 Y_*(J)=D_JK(J)-c_*q,
 \qquad c_*=-252,
\]

before taking its history norm or applying the adjoint row.  The current
voltage coordinates of $q$, $D_JK$, and $Y_*$ vanish exactly, so all
three lie in $\Sigma$.  Since $f(q)=1$,

\[
 \begin{aligned}
 P_sD_J\kappa=P_sD_JK
 &=D_JK-qf(D_JK)\\
 &=Y_*+q\bigl(c_*-f(D_JK)\bigr).               \tag{3.1}
 \end{aligned}
\]

This is the relevant stable component.  It is unrelated to the terminal
one-step stable-map diagnostic sometimes denoted $M_s$ in Stage 4J.

## 4. Directed norm bound

The source replays the Stage-4E Route-C section eigencolumn.  It takes the
larger of the voltage-history and current-recovery Wiener majorants, adds the
complete section-column error, and retains one further transcription guard.
The Stage-5E phase rotation has unit modulus, hence the same bound holds for
the real physical column:

\[
 \|q_{\rm phys}\|_Y
 \le 1.38298060370245631841470282396581026559885598345994672569132122.
                                                               \tag{4.1}
\]

Together with the released Stage-5E bounds

\[
 \|Y_*(J)\|_Y
 \le 5.397270394398794158766200979646630123\ldots,
\]

\[
 |c_*-f(D_JK(J))|
 \le 6.746521015804618914787719270270463549\ldots,
\]

identity (3.1) gives uniformly on the full physical pulse interval

\[
 \|P_sD_J\kappa(J)\|_Y=\|P_sD_JK(J)\|_Y
 \le 14.727578101727574870594060320513795141\ldots .          \tag{4.2}
\]

All registered endpoints are generated by 192-bit outward MPFR arithmetic.
The validator reconstructs every downstream expression from the displayed
outward endpoints, rather than relying on hidden precision.

## 5. Quantitative conditional gate

Differentiating the correctly centered gap (2.1) yields

\[
 H'(J)=f(D_JK)-D\psi(P_s\kappa(J))[P_sD_JK].                  \tag{5.1}
\]

Stage 5E proves

\[
 f(D_JK(J))\in
 [-258.7465210158046\ldots,-245.2534789841953\ldots].        \tag{5.2}
\]

Consequently, if a future certificate proves in the identical centered chart
that

\[
 \kappa(I_J)\text{ lies in the ambient chart and }
 P_s\kappa(I_J)\subset\operatorname{dom}\psi,
 \qquad
 \sup_{J\in I_J}\|D\psi(P_s\kappa(J))\|\le16,               \tag{5.3}
\]

then outward interval arithmetic gives

\[
 H'(J)\in[-494.3877706434458\ldots,-9.6122293565541\ldots]
 \subset(-\infty,0).                                         \tag{5.4}
\]

The maximum admissible graph-derivative norm is bounded below by
$16.6526687069767\ldots$, so the target 16 retains a strict margin.

## 6. Exact logical boundary

- **PROVED:** the common-coordinate projection identity (3.1), the physical
  column norm (4.1), the stable derivative bound (4.2), and implication
  (5.3) $\Rightarrow H'<0$ for the selected Stage-5C event.
- **OPEN antecedents:** construction of a quantitative centered $C^1$
  stable graph in this exact normalization, ambient-chart containment of
  $\kappa(I_J)$, domain containment of $P_s\kappa(I_J)$, and the derivative
  estimate in (5.3).
- **NOT CLAIMED:** ordinal third crossing, gap endpoint signs, interval-Newton
  inclusion, a unique $J_c$, physical onset, two-sided routing, basin
  capture, asynchronous network safety, or a frequency-amplitude-safety
  radius.

The result artifact uses exact top-level and nested schemas, hashes both
parents and all source files, validates both parent artifacts at every normal
ingress, binds the parameter interval, centered chart and coordinate
normalization as theorem data, checks the current Python/MPFR/NumPy/SciPy and
thread environment against its manifest, and clears the source-bound
eigencolumn cache before a fresh replay.
