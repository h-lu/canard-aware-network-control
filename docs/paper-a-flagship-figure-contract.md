# Paper A flagship figure contract

- Reader question and result served: how can a delay redistribution that is
  annihilated by the stationary projection move a fixed-model complete-history
  heteroclinic canard, and which part of the answer is robust across anchors?
- Status: **MIXED**. Panel (a) is a schematic history-space channel populated
  by proved invariant objects. Panel (b) gives exact identities and estimates
  in a schematic dependency layout. Panel (c) is a schematic parameter-plane
  realization of the proved centering and conormal limit.
- System, parameters, coordinates, and time convention: the globally anchored
  shared-resource RFDE; forward original time in panel (a); structural
  direction `eta=zeta R` and physical parameter `mu` in panel (c).
- Objects and directions: `E_N^+`, its inward one-dimensional unstable branch,
  attracting and repelling slow-history tracking segments, the central fold
  passage, the intrinsic stable history sheet of `E_N^-`, and forward-time
  orientation. The positions, lengths, and angles are not quantitative.
- Panels and reading order: (a) the complete-history connection in one fixed
  anchored RFDE; (b) the blind-source/return/root factorization and the
  one-delay no-go; (c) different absolute anchor baselines followed by
  modelwise centering and a common limiting conormal.
- Visual language: blue solid = incoming/attracting segment; vermilion
  dash-dot = outgoing/repelling segment; black = algebraic mechanism or
  anchor A; gray dashed = comparison, one-delay no-go, or anchor B. Every
  color distinction also has a line-style or direct-label cue.
- The figure must not imply: that the original unanchored recovery law has the
  displayed equilibria; that the drawing proves existence; that the orbit is
  planar; that exact roots agree across anchors; that one delay prevents every
  possible higher-order response; or that the full network is reconstructed.
- Production route: reproducible Matplotlib vector PDF from
  `manuscript/network-root-transfer/figures/root_mechanism.py` using
  `make figure` in the manuscript directory.
- Final placement and size: one full-text-width figure immediately after the
  flagship theorem and before the anchor/preparation distinction.
- Checks: regenerate deterministically; inspect standalone PDF; verify vector
  content and embedded fonts; inspect the rendered manuscript page at final
  size; check grayscale line-style distinctions, arrow direction, labels,
  crop, and caption claims.
